# Flowlearn MCP credentials.
# Copy to `.env` (in this directory) and fill in real values.
# `.env` is gitignored — never commit real credentials.

FLOWLEARN_EMAIL=
FLOWLEARN_PASSWORD=
FLOWLEARN_TENANT_SLUG=
